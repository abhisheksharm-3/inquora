/** Web results per search. */
export const MAX_WEB_RESULTS = 5;

/** The ceiling on what a caller may ask for. */
export const MAX_WEB_RESULTS_REQUESTED = 10;

/** A search that has not answered in this long is not going to help the answer. */
export const WEB_TIMEOUT_MS = 20_000;
